package main

import "fmt"

func main() {
	var a, b, c int

	fmt.Scanf("%d \n", &a)
	fmt.Scanf("%d \n", &b)
	fmt.Scanf("%d \n", &c)

	if (a * a) + (b * b) = c * c && (a * a) + (c * c) = b * b && (c * c) + (b * b) = a * a{
		fmt.Println("равностороннии треугольник")
	}

}
